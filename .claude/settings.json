{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(xcodebuild build *)",
            "command": "INPUT=$(cat); echo \"$INPUT\" | jq -r '.tool_response.stdout // \"\"' | grep -q 'BUILD SUCCEEDED' && /Users/robertwang/Develop/gmail-client/scripts/build-dmg.sh || true",
            "async": true,
            "statusMessage": "Building DMG..."
          }
        ]
      }
    ]
  }
}
